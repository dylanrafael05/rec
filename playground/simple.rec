external fn puts(str *u8)
external fn malloc(sz usize) *none

mod sys
{
    fn println(str [u8]) 
    {
        puts(str.ptr);
    }
}

fn main() i32
{
    sys::println("Hello, world!\n");
    return 0;
}