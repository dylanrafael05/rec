// Module definition
mod example
{
    // Simple type definition
    struct Type { x isize }

    // 'Constructor'
    fn new_type() Type
    {
        return struct Type { x = 0iz };
    }
}

// Usage example
fn use_type() example::Type
{
    let unused = example::new_type();
    return example::new_type();
}