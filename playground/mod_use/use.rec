use math

fn main()
{
    // NOTE: definitely returns analysis is required to ensure
    // void functions return.

    let x = Vector2::make(0.51, 0.0);
    let y = x.get_x().x;
    
    if y == 0.0
    {
        let z = Vector2::make(0f32, 0f32).get_x().x;
    }

    return;
}