mod math

struct Vector2
{
    x f32,
    y f32
}

mod as Vector2

fn make(x f32, y f32) Vector2
{
    return new Vector2
    {
        x = x,
        y = y
    };
}

fn get_x(self) Vector2
{
    // TODO; maybe allow errors through until the last step?
    // TODO; permit 'self' as an expression
    return 0.0;
}